#include "main.h"
#include <stdio.h>
/**
 * main - principal funtion
 * @argc: Int que cuenta los strings
 * @argv: Direccion
 * Return: 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
		printf("%s\n", argv[0]);
			return (0);
}
